# SET
# Immutable, non-ordered, no index based,(), No duplicates

my_list = [1, 5, 8, 2, 3, 5]
my_list = ["Apple", "Banana", "Orange"]
my_set = set(my_list)
print(my_set)

# # first element of set (Not worked)
# print(my_set[0])

# change the value of tuple (not worked)
#my_set[0] = 100